/opt/vault/data/audit.log {
    weekly
    maxsize 100M
    rotate 8
    missingok
    notifempty
    compress
    delaycompress
    nocreate
    postrotate
        /usr/bin/docker kill -s HUP vault >/dev/null 2>&1 || true
    endscript
}
